services:
  db:
    container_name: db
    image: mysql:9.6
    environment:
      - MYSQL_DATABASE=postcredits
      - MYSQL_ROOT_PASSWORD=password
    volumes:
      - mysql:/var/lib/mysql
    ports:
      - "3306:3306"
volumes:
  mysql: